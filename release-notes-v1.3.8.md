## v1.3.8 更新内容

### 🚀 重写并发压力测试（对照业界标杆：AWS 饱和测试 / Gatling 爬坡 / LiteLLM 限流模型）

用户实测：DeepSeek V4 官方 2500 并发、但动态测试测出 RPM 72，怀疑测不准。审查发现旧逻辑三大硬伤，按业界做法重写：

- **并发线性爬坡（逐档 +1 爬到顶 64）**：替代「固定 14 并发灌 RPM 窗口」——固定并发永远测不到高并发 API 的顶（DeepSeek 2500 并发被固定 14 压不满，误把 65s 发送量当 RPM）；翻倍档位（2,4,8,16）断档太粗，真实顶 30 会爬到 32 撞了回退 16 浪费一半。线性逐档精确测到顶
- **固定轻负载（每档 8 条）测并发承受力**：替代「阶加累积负载（1+2+3…cc）」——阶加让慢 API 每档 payload 越来越重（并发 10 发 160 条要 18s），180s 预算只爬到并发 10 就耗尽、永远测不到真实顶（本喵用 step-3.7-flash 实测验证）。固定轻负载是业界饱和测试标准：每阶段固定负载、递增并发、观察吞吐拐点
- **RPM 闸区分「并发型」vs「RPM 型」**：并发型 API（DeepSeek 官方 2500 并发、无 RPM 限流）爬坡测出真实吞吐、RPM 按该并发反推、闸放行不成为瓶颈；RPM 型 API（MiniMax/百炼）爬坡撞 429 自动停在配额内档位
- **RPM 反推封顶 _AUTO_MAX_RPM(10000)**：快 API 单批 0.02s 会反推 19 万 RPM——写进 config 前端显示荒谬值、RateGate 闸形同虚设，封顶才准确
- **并发上限 16 → 64**（前端滑块同步）：高并发 API 能顶满
- **`_measure_w` 失败不再拦截爬坡**：慢 API 基准测量超时不会误杀（爬坡并发 1 起步、120s 超时更宽容）
- **RateGate auto 上限 300 → 10000**：并发型 API 不被闸卡死（64 并发 × 单批 1s 需 3840 RPM，300 上限是硬瓶颈）

### 验证

- 后端 **422 测试全绿**（新增 6 个爬坡测试：并发型爬到顶 / RPM 型精确停在限流前 / 超时型 / 全失败 / 慢 API 固定轻负载爬到顶 / RPM 封顶）
- **真实 step-3.7-flash 实测**：从旧逻辑「并发 10（阶加膨胀）」提升到「并发 17（固定轻负载）」——慢 API 也能爬到更高并发
- 端到端：stepfun 端点 `/step_plan/v1` 确认（`/step_plan` 少了 /v1 会 404）

**安装包**：便携版双击即用；安装版走安装向导。
