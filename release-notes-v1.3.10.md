## v1.3.10 更新内容

### 🚀 慢 API 动态测试彻底优化（用户实测：阶跃星辰 step-3.7-flash 动态测试超时/不可用）

对照业界标杆（litellm adaptive timeout / p95 延迟 + 余量校准超时 / AWS 饱和测试拐点法）重构并发压力测试，适配所有 API：

**实测对比（step-3.7-flash 真实 API）**：

| | 优化前 | 优化后 |
|------|--------|--------|
| 测出并发 | 19（预算耗尽） | **64（顶满）** |
| 耗时 | 210s（3.5 分钟） | **47s**（快 4.4 倍） |
| 结果 | 「未撞限流」误导 | 「爬到上限 64」准确 |

**核心改动**：

1. **多档并行爬坡**（业界 litellm 并发快照思路）：所有并发档位**同时发请求**、每档独立判定——慢 API 总耗时从「档数 × 单批」降到「~2 × 单批」。stepfun 单批 9.7s，串行 16 档要 155s、并行 ~20s 全部完成
2. **自适应超时**：`timeout = max(120, W×5)`——按基准 W 动态设，慢 API 单批波动（30s+）不再误判超时「不可用」
3. **自适应爬坡步长**：W 大（慢 API）→ 大步爬（跳过中间档省时间）；W 小（快 API）→ 细爬测准
4. **自适应预算**：慢 API 给更长预算（单批 9s 需 ≥300s），快 API 180s 够
5. **消息语义修正**：并发 < 上限且未撞限流 → 提示「慢 API 预算内稳定下限，可填更高配额」；爬到上限 → 准确报「并发到顶」

### 验证

- 后端 **428 测试全绿**（新增：慢 API 并行爬坡到顶测试）
- 真实 step-3.7-flash 实测：并发 64、RPM 193、47s 完成——不再超时/不可用

**安装包**：便携版双击即用；安装版走安装向导。
