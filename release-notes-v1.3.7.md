## v1.3.7 更新内容

### 🐛 重写进度计数板块（根治「进度超 total / 进度条随实际变化」）

用户实测：进度条出现 `4,895/4,588`——done 超 total、total 还被拉高掩盖。重写计数体系，从结构上保证 **done 永不超 total、total 一次性定死不跟随**：

- **账本式 done 计数（防双计）**：`_bump_stage` 带唯一键（(归属, key)），同一 key 重复 bump 直接忽略。此前记忆命中条目「入队审查 + 提前 bump」被计 2 次（整合包大量记忆命中 → done 虚高，4,895/4,588 超出的 307 根因），现在任何路径双计都被账本吞掉
- **修复记忆命中双计**：入队审查管道的记忆命中分支不再提前 bump（对齐简繁分支 v1.2.9 修复，此前漏修）
- **total 一次性定死**：扫描后 total = lang + json + pack + hardcode 全部翻译条目，**删除三处 `max(done+failed)` 跟随**——total 不再被 done 拉高（「进度条怎么能够随实际变化」根因）
- **build 剥离全局进度**：打包产物单位不再计入全局 done/total，build 只推进自身 stage 明细，翻译完成即 100%
- **前端显示双保险**：进度条分母取 `max(total, done)`，数字 + 百分比都绝不超限

### 🐛 修复「翻译置换」（用户实测：Deforester 条目被翻成幸运Ⅰ工、Fortunate II 被翻成强化可控爆破装药）

- **占位符交叉校验（核心）**：翻译批量解析后写回前，对每条「原文 ↔ 译文」做占位符一致性校验——AI 输出 [iN] 标签与内容错位（A 条目译文被标到 B 条目 key 上）时，该条原文含的占位符（`$(item)`/`$()`/`%s` 等）在译文里必然缺失或混入他人占位符 → 判定疑似置换 → **降级单条重翻**（单条独立请求，AI 物理上无法再串位），杜绝置换写进产物
- **`$(...)` Parchment 富文本占位符保护**：`$(item)`/`$()`/`$(bold)`/`$(o)` 等排版标记此前未被保护，AI 把 `$()` 破坏成 `$O`/`$0`（用户实测「$(item)强化可控爆破装药$O…」乱码）——现在整体保护，翻译中间文本、还原无损
- **审查规则强化「张冠李戴」**：译文描述内容与源文本完全无关（像另一条条目的译文）必标记不合格——第二道防线，兜底无占位符纯文本的置换
- **prompt 强化序号对应**：明确「序号 [iN] 必须与输入严格一一对应、不得调换条目顺序（串位=翻译失败）」

### 验证

- 后端 **416 测试全绿**（新增账本幂等、Parchment 保护、置换拦截、不误伤测试）
- 端到端 mock 复现用户两个场景：置换已拦截、`$()` 无损还原；记忆命中双计根治（done == total）

**安装包**：便携版双击即用；安装版走安装向导。
