## v1.4.6 更新内容

### 🎯 核心修复（流畅使用保障）
- **并发压力测试失败**：64档同时 gather 打同一 API 会互相挤占（瞬时并发≈2080），改为**分波并行**（每波8档），测出真实并发
- **卡在「正在翻译语言文件」**：`_prebuild_terms` 术语预扫描同步正则阻塞事件循环，改为后台线程 + 超时
- **翻译极慢**：撤销并发池分离，恢复全局共享 `_conc_sem`（deepseek 并发能力强不再被限制到1/4）
- **API Key 无效不提示**：401/403 fatal 被异常路径吞掉，改为 `_engine_translate` 捕获返回 fatal meta

### 🐛 Bug 修复
- **硬编码翻译静默失败**：不传 meta 无法区分「AI保留原文」和「请求失败」，失败全被吞掉 → 传 meta 精确计 failed
- **硬编码引擎连接池泄漏**：任务结束不关闭 httpx 客户端 → finally 补 aclose
- **重试实际等待远超90s**：单次请求可阻塞180s → wait_for 包住按剩余时间截断
- **断点续联指纹不一致**：`_check_resume` 对包裹目录未下钻 → 与 run() 指纹对齐
- **onUnmounted 覆盖配置**：冷启动后端未就绪时用默认值覆盖已有配置 → 加 loaded 守卫
- **4xx 切批重试浪费**：rejected 类错误直接记 failed，不再15次空转重试
- **acquire 后未复查 fatal**：等令牌期间其他请求撞401后仍白打请求 → 复查
- **httpx 连接池不随热更新重建**：改并发时重建客户端
- **前端吞吐测试超时**：180s→300s（慢 API 测试可达270s）

### 📦 打包说明
- 便携版：`release-v1.4.6-portable.exe`（单文件，免安装）
- 安装版：`release-v1.4.6-setup.exe`（需 Inno Setup 6）
