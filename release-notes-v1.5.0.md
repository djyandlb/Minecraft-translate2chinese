## v1.5.0 更新内容

大版本更新：审查与翻译全面提速、令牌协调优化、一批稳定性与兼容性修复。

---

### 🚀 审查与翻译提速

- **审查多 consumer 并行**
  原单一审查 consumer 串行处理，产出卡在「20 条 / 审查耗时」。现在多个审查 consumer 并行，产出速度按并发数翻倍。

- **审查 consumer 取消封顶**
  审查与翻译共享全局 RateGate 令牌桶，consumer 数量 = 设置并发。翻译管道空下来时，空闲令牌立即喂给审查，令牌高效利用。令牌桶按配额放行，consumer 再多也不会触发 API 限流。

- **审查批大小跟随设置**
  审查攒批不再固定 20 条，改为跟随设置里的批大小。请求数减半，单请求 token 消耗更平滑，速度快起来不容易撞 API 的 token 配额。

- **审查独立并发信号量**
  审查不再被翻译占满共享并发池挤占，两条管道并行不打架。

- **术语预扫描后台异步**
  翻译前预扫描术语表改为后台执行，不再阻塞主翻译，解决「正在翻译语言文件」卡住。

---

### ⚡ 令牌协调

- **RateGate 桶容量收紧**
  桶容量限到 6 秒配额，防止满桶突发超速触发 API 限流。

- **429 全局协调冷却**
  任一请求撞 429 时全局暂停放行，冷却后按新速率慢启动，尊重 Retry-After 头，杜绝各自退避重试风暴。

- **RPM 预算闸自动校准**
  默认自动学习 API 的限流配额，撞限流自动退档、稳定自动升档，每个 API 都能稳跑满速。也可手动填写已知配额。

---

### 🛠️ 稳定性与兼容性修复

- **1.21.4 整合包材质包版本不兼容**
  产物 pack_format 曾写错被 1.21.4 游戏拒载。整合包版本检测改众数投票：遍历全部 mods jar 统计版本声明，多数 mod 声明的主流版本胜出，1.21.4 正确写入 pack_format 46。

- **shader 与程序资源通用排除**
  汉化 Sodium 等 mod 时，`shaders/` 配置曾被当文本翻译，资源路径、uniform 名变中文导致游戏加载崩溃。新增通用排除清单：shaders、shaderpacks、models、textures、blockstates、animations、particles、sounds、font、icons 一律跳过，永不翻译。

- **资源包图标丢失**
  打包后找不到 pack.png 导致游戏显示默认图标。改为多候选路径查找，兼容 PyInstaller 不同版本的目录布局。

- **断点续联判定放宽**
  取消或中断的任务也能显示可续联，前端取消后实时刷新续联列表，不再只有重启应用才能续联。

- **硬编码判断完成计数**
  判断完成数改为重判完全结束后一次性计入，并新增「已处理 X/N」进行中反馈，进度明细持续活动不卡死。

- **modjar 构建逐 jar 进度**
  打包阶段逐 jar 显示「正在构建汉化 jar i/N」，不再干等。

- **其他电脑打开白屏修复**
  前端构建目标降到 es2017，兼容老版 WebView2 Runtime。老 WebView2 跑不动 Vue3 的现代 JS 会解析失败白屏，降级转译后旧系统也能正常显示。同时桌面壳启动时检测 WebView2，缺失则弹中文提示引导安装，不再静默白屏。

---

### 📖 内置使用说明更新

- README 版本号更新至 v1.5.0，吞吐校准章节重写，说明「动态测试吞吐」与 RPM 预算闸自动校准。
- 设置页吞吐提示、应用内「使用说明」补吞吐校准步骤。
- 全量删除小括号隐性阅读文本。

---

### 📦 打包说明

- 便携版：`release-v1.5.0-portable.exe`，单文件，免安装
- 安装版：`release-v1.5.0-setup.exe`，需 Inno Setup 6
